export const mapRoutes = (route) => {
    switch (route) {
        case '/':
            return 0;
            break;
        case '/services':
            return 1;
            break;
        default:
            return 2;
    }
};
//# sourceMappingURL=router.js.map